const path = require("path");

module.exports = {
  config: path.resolve(__dirname, "configs", "sequelize.config.js"),
  "models-path": path.resolve(__dirname, "models"),
  "migrations-path": path.resolve(__dirname, "migrations"),
};
